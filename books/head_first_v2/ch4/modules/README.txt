test module
===========

1. Generate by opening containing folder and running `python3 setup.py sdist`
1. Install by opening generated `dist` folder and running `python3 -m pip install lsearch-1.0.tar.gz`